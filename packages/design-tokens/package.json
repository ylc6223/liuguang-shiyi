{
  "name": "@packages/design-tokens",
  "version": "0.0.0",
  "private": true,
  "description": "Design tokens (color, spacing, typography) as a single source of truth.",
  "scripts": {
    "build": "echo 'Emit tokens to CSS/TS via Style Dictionary'",
    "dev": "echo 'Watch token sources and rebuild'"
  }
}

